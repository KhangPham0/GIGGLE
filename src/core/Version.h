#ifndef GIGGLE_CORE_VERSION_H
#define GIGGLE_CORE_VERSION_H

namespace giggle {

// The application version, taken from the CMake project version.
const char* Version();

} // namespace giggle

#endif
